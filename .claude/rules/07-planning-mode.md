# 07. Planning Mode & Mentorship Blueprint

> Pointer file. Same precedence tier as every other `.claude/rules/*.md`
> file — listed in `06-escalation-protocol.md`. The full planning,
> mentoring and code-unlock workflow moved into one skill on 2026-09-25 at
> the user's request, so there is one unified workflow instead of two
> overlapping ones.

When the user starts a prompt with **`PLAN:`**, types **`/aeryo-mentor`**, or
unlocks code with **`WRITE CODE`** / **`WRITE CODE FOR STEP X`** in that
thread, follow `.claude/skills/aeryo-mentor/SKILL.md`. That file is this
rule's content and carries this rule's precedence tier.

Cursor loads the skill file directly through `.cursor/rules/aeryo-stack.mdc`.
