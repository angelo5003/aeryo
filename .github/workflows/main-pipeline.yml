name: Production Pipeline

on:
  push:
    branches:
      - main

# Zorgt ervoor dat oude builds stoppen als je snel achter elkaar pusht
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  database:
    name: Supabase Migrations
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Supabase CLI
        uses: supabase/setup-cli@v1
        with:
          version: latest
      - name: Push Migrations
        # GitHub Actions runners are IPv4-only; direct db.<ref>.supabase.co:5432
        # is IPv6-only without the paid IPv4 add-on. Route through Session
        # Pooler (port 5432, IPv4 on every tier) instead.
        # per https://supabase.com/docs/guides/database/connecting-to-postgres
        run: supabase db push --db-url "${{ secrets.SUPABASE_DB_POOLER_URL }}"

  web-deploy:
    name: Web Deployment (Vercel)
    runs-on: ubuntu-latest
    needs: database # Wacht met Vercel tot de database klaar is
    # Hier vertellen we de hele job wat de Vercel ID's zijn
    env:
      VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
      VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
    steps:
      - uses: actions/checkout@v4
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"
      - name: Install dependencies
        run: npm ci
      - name: Install Vercel CLI
        run: npm install --global vercel@latest
      - name: Pull Vercel Environment
        run: vercel pull --yes --environment=production --token=${{ secrets.VERCEL_TOKEN }}
      - name: Build Next.js App
        run: vercel build --prod --token=${{ secrets.VERCEL_TOKEN }}
      - name: Deploy to Vercel
        run: vercel deploy --prebuilt --prod --token=${{ secrets.VERCEL_TOKEN }}

  android-build:
    name: Android Native Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"
      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: "zulu"
          java-version: "17"
          cache: "gradle"
      - name: Install dependencies
        run: npm ci
      - name: Sync Capacitor Android
        run: npm run android:sync
      - name: Build Android APK
        working-directory: ./android
        run: ./gradlew assembleRelease
      - name: Upload Android Artifact
        uses: actions/upload-artifact@v4
        with:
          name: aeryo-android-apk
          path: android/app/build/outputs/apk/release/app-release.apk

  ios-build:
    name: iOS Native Build
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"
      - name: Install dependencies
        run: npm ci
      - name: Sync Capacitor iOS
        run: npm run ios:sync
      - name: Cache CocoaPods
        uses: actions/cache@v4
        with:
          path: ios/App/Pods
          key: ${{ runner.os }}-pods-${{ hashFiles('ios/App/Podfile.lock') }}
          restore-keys: |
            ${{ runner.os }}-pods-
      - name: Install CocoaPods
        working-directory: ./ios/App
        run: pod install
      - name: Build iOS App
        working-directory: ./ios/App
        run: xcodebuild -workspace App.xcworkspace -scheme App -configuration Release -sdk iphoneos build CODE_SIGNING_ALLOWED=NO
      - name: Upload iOS Artifact
        uses: actions/upload-artifact@v4
        with:
          name: aeryo-ios-app
          path: ios/App/build/Products/Release-iphoneos/App.app

  release:
    name: Create GitHub Release
    runs-on: ubuntu-latest
    needs: [web-deploy, android-build, ios-build]
    steps:
      - uses: actions/checkout@v4
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Semantic Release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: npx semantic-release
