import type { TextareaProps as ChakraTextareaProps } from "@chakra-ui/react";

/**
 * All of Chakra's `Textarea` props (the same `variant`/`size` recipe
 * shape as `Input`, style/responsive props, `as`/`asChild`). A pure
 * pass-through, same reasoning as `Input`.
 */
export type TextareaProps = ChakraTextareaProps;
