import type { CenterProps as ChakraCenterProps } from "@chakra-ui/react";

/**
 * All of Chakra's `Center` props — every style/responsive prop and
 * polymorphic `as`/`asChild`. `Center` is a pure pass-through: this
 * interface exists so consumers import the type from AERYO, not Chakra,
 * directly.
 */
export type CenterProps = ChakraCenterProps;
