import type { SeparatorProps as ChakraSeparatorProps } from "@chakra-ui/react";

/**
 * All of Chakra's `Separator` props — `orientation`, every style/
 * responsive prop, and polymorphic `as`/`asChild`. `Separator` is a pure
 * pass-through: this interface exists so consumers import the type from
 * AERYO, not Chakra, directly.
 */
export type SeparatorProps = ChakraSeparatorProps;
